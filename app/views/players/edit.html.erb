<h1>Editing <%= @player[:first_name].capitalize %> <%= @player[:last_name].capitalize %></h1>

<%= render 'form' %>

<div class="col-md-12">
<%= link_to 'Show', @player %> |
<%= link_to 'Back', players_path %>
</div>
